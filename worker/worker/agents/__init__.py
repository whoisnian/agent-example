"""Per-task agents.

Empty at the scaffold stage. Real agents (``code_agent``, ``research_agent``,
...) are added by downstream OpenSpec proposals. The ``ExecutionDispatcher``
in ``worker.core.dispatcher`` always raises ``AgentNotImplementedError`` until
an agent is wired here.
"""
