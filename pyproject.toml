[project]
name = "agent-example"
version = "0.1.0"
description = "A deep agent for various tasks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dashscope>=1.25.13",
    "deepagents>=0.4.11",
    "langchain>=1.2.12",
    "langchain-community>=0.4.1",
]
